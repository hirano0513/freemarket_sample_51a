.personal-entry
  .signup-page-header
    %header.personal-entry__header
      = render "layouts/entry-header"
      %nav.progress-bar.signup-bar
        %ol.clearfix
          %li.through
            会員情報
            .progress-status
          %li.active
            電話番号認証
            .progress-status
          %li
            お届け先住所入力
            .progress-status
          %li
            支払い方法
            .progress-status
          %li
            完了
            .progress-status
  %main.personal-entry-main
    %section.entry-container
      %h2.entry-container__head.entry-container__head--sms
        電話番号の確認
      .single-inner.entry-form
        .person-content
          = form_for @sms do |f|
            %ul
              %li
                = f.label :"携帯電話の番号"
                = f.text_field :"phone-number", placeholder: "携帯電話の番号を入力",class: "input-default"
                %p 本人確認のため、携帯電話のSMS(ショートメッセージサービス)を利用して認証を行います。
              %li.actions
                = link_to "SMSを送信する",users_address_path, class:"btn-default btn-red"
                %p ※電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。
              %li.text-right
                = link_to root_path do
                  電話番号の確認が必要な理由
                  %i.fas.fa-chevron-right
  = render "layouts/entry-footer"
