= render 'layouts/header'
.credit-card-edit
  .bread-crumbs
  .l-container.clearfix
    .l-content
      %h2 購入が完了しました！
      %p #{@item.name}
      %p ¥#{@item.price.to_s(:delimited, delimiter: ',')}(送料込み)
    = render 'render/sidelist'
= render 'layouts/footer'

= render 'render/footer-sell-btn'