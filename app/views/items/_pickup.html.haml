%section.section
  = link_to '#' do
    %figure.section__image
      = image_tag "#{item.images[0].image}", class: 'item-image'
    .content
      %h3.content__title.font-2 #{item.name}
      .item-box
        .item-box__price.font-5 ¥#{item.price}
        .item-box__mask.font-2
          %i.far.fa-heart
          %span #{item.like}
      %p.content__lead (税込)
